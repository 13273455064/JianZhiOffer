package com.liquor.offer.no31to40;

/**
 * 统计一个数字在排序数组中出现的次数。
 *
 * 排序的数组，用二分查找
 */
public class No39 {

    public int GetNumberOfK(int [] array , int k) {

        if (array.length == 0){
            return -1;
        }

        return 0;
    }
}
